## June 27, 2026

- Added quick application/dashboard to the Pi project so there is something visible on the screen.
- Created a basic app shell to run on the Pi. No Morse training architecture yet.
- Added a newer touch-first Dit Dit app. This work has since been promoted into the active `app/` directory.
- Current Dit Dit app direction includes:
  - `Practice`
  - `Settings`
  - `Exit to Desktop`
- At the time, `Practice`, `Settings`, and `Exit to Desktop` were placeholders. The active app now includes browser Morse audio, local settings, local progress history, missed-character practice, and an exit flow foundation.
- Current structure note: `app/` is the active Dit Dit app. The older dashboard prototype is archived under `legacy/app-prototype/`.
- Current Pi note: `deploy/pi/` contains the Dit Dit Box appliance/kiosk layer, and its Compose setup builds the active `app/`.
- Naming note: `Dit Dit` is the React/Vite CW trainer app/product. `Dit Dit Box` is the Raspberry Pi appliance/kiosk deployment of Dit Dit.
- Kiosk note: when the Pi restarts, the user should see that Dit Dit is about to launch and should have a chance to cancel to desktop before kiosk mode starts.

---
### Started MVP dev with copilot. 
