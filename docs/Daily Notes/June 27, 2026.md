## June 27, 2026

- Added quick application/dashboard to the Pi project so there is something visible on the screen.
- Created a basic app shell to run on the Pi. No Morse training architecture yet.
- Added a newer touch-first Dit Dit app under `app/ditdit/`.
- Current Dit Dit app includes:
  - `Practice`
  - `Settings`
  - `Exit to Desktop`
- `Practice`, `Settings`, and `Exit to Desktop` are placeholders for now.
- Important structure note: the repo currently contains both the older `app/src/` dashboard and the newer `app/ditdit/src/` app.
- Important Docker note: `docker-compose.yml` still builds from `./app`, so the Docker setup may still point at the older app unless updated.
- Next decision: make `app/ditdit/` the official app home or move the Dit Dit app back up into `app/`.
- Next technical step: align Docker, README, AGENTS.md, and Obsidian docs with the chosen app structure.
- Kiosk note: when the Pi restarts, the user should see that Dit Dit is about to launch and should have a chance to cancel to desktop before kiosk mode starts.

---
### Started MVP dev with copilot. 